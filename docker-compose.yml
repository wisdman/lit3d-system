version: "3.9"

services:
  admin:
    restart: unless-stopped
    build:
      context: ./
      args:
        - APP_PATH=packages/admin
    image: lit3d-system/admin
    networks:
      default:
    ports:
      - 80:80
      - 443:443